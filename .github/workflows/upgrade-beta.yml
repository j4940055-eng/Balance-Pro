name: Upgrade MoneyTalks beta
on:
  push:
    branches: [main]
    paths:
      - scripts/upgrade-index.py
      - scripts/pwa-index.py
      - .github/workflows/upgrade-beta.yml
  workflow_dispatch:
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply architecture and UI upgrade
        run: |
          python scripts/upgrade-index.py
          python scripts/pwa-index.py
      - name: Commit upgraded app
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "Already upgraded"
          else
            git commit -m "Upgrade MoneyTalks beta architecture and PWA"
            git push
          fi
